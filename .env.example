# Seafile Docker Environment Variables
# Copy this file to .env and customize for your environment

# === Volume Directories ===
SEAFILE_VOLUME=/volume1/docker/seafile/seafile-data
SEAFILE_MYSQL_VOLUME=/volume1/docker/seafile/mysql
SEAFILE_ELASTICSEARCH_VOLUME=/volume1/docker/seafile/elasticsearch

# === MySQL Configuration ===
# Root password for MySQL (only required on first deployment)
# IMPORTANT: Change this to a strong, unique password!
INIT_SEAFILE_MYSQL_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# MySQL Database Configuration
SEAFILE_MYSQL_DB_HOST=db
SEAFILE_MYSQL_DB_PORT=3306
SEAFILE_MYSQL_DB_USER=seafile
# IMPORTANT: Change this to a strong, unique password!
SEAFILE_MYSQL_DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
SEAFILE_MYSQL_DB_CCNET_DB_NAME=ccnet_db
SEAFILE_MYSQL_DB_SEAFILE_DB_NAME=seafile_db
SEAFILE_MYSQL_DB_SEAHUB_DB_NAME=seahub_db

# === Seafile Server Configuration ===
# JWT Private Key - Generate with: pwgen -s 40 1 (or openssl rand -base64 32)
# REQUIRED: A random string with a length of no less than 32 characters
# IMPORTANT: This must be a cryptographically random string!
JWT_PRIVATE_KEY=REQUIRED_CHANGE_ME_USE_PWGEN_OR_OPENSSL

# Server hostname or domain (REQUIRED)
# Change this to your actual domain
SEAFILE_SERVER_HOSTNAME=seafile.shojinas.home.shoji.me

# Server protocol (http or https)
# Use http when behind Traefik reverse proxy
SEAFILE_SERVER_PROTOCOL=http

# === Cache Configuration ===
# Cache provider: redis (recommended) or memcached
CACHE_PROVIDER=redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# === Time Zone ===
TIME_ZONE=America/Toronto

# === Notification Server ===
# Enable (true) or disable (false) notification feature
ENABLE_NOTIFICATION_SERVER=false
NOTIFICATION_SERVER_URL=

# === Admin Account (First Deploy) ===
# Initial admin username and password
# IMPORTANT: Change this to a strong, unique password!
INIT_SEAFILE_ADMIN_EMAIL=admin@example.com
INIT_SEAFILE_ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# === Advanced Settings ===
# Run Seafile container without a root user
NON_ROOT=false

# Maximum number of files for metadata feature
MD_FILE_COUNT_LIMIT=100000

# === User/Group IDs ===
PUID=1027
PGID=100

# === Tailscale Configuration ===
# Tailscale auth key - get from https://login.tailscale.com/admin/settings/keys
# Generate an auth key with "Reusable" and "Ephemeral" options for Docker containers
TAILSCALE_AUTHKEY=tskey-auth-CHANGE_ME
# Hostname for this container on your tailnet (optional)
TAILSCALE_HOSTNAME=seafile
# Additional Tailscale arguments (optional)
# Example: --accept-routes --advertise-exit-node
TAILSCALE_EXTRA_ARGS=
# State directory for Tailscale
TAILSCALE_STATE_DIR=/volume1/docker/seafile/tailscale
